# First-party packages under the coverage gate (CONSTRAINTS.md).
# One pattern per line, relative to the module root. Comments allowed.
./internal/brand
./internal/id
./internal/ai/capability
./internal/completion
./internal/scheduler
# ./internal/fakecoder   # add in the same commit the fake coder lands
